# Amazon New Releases Collector — Task Board

更新时间：2026-09-23

## 当前结构

```text
单一配置页
    ↓
每日调度器
    ↓
Amazon 新品榜采集
    ↓
最近 7 天 SQLite 快照
```

## 已完成

- [x] 单一 `/` 配置页。
- [x] 配置每日运行时间，默认 `06:00`、时区 `Asia/Shanghai`。
- [x] 在配置页启用或停用榜单数据源。
- [x] 在配置页新增美国站或德国站榜单类目。
- [x] 可视化快照量、唯一 ASIN、最近状态和近 7 天采集量。
- [x] 每天最多自动执行一次。
- [x] 只保存采集事实：`observations`、`product_seen` 和 `collection_runs`。
- [x] 快照最多保留 7 天。
- [x] `product_seen` 长期保留，支持准确判断 ASIN 首次出现。
- [x] 每个数据源记录 `RUNNING`、`COMPLETE` 或 `FAILED` 采集结果。
- [x] 删除旧数据库、旧页面、旧 API、旧任务模型和兼容迁移。
- [x] 保留访问控制检测，遇到登录或验证码立即停止。

## 待验证

- [ ] 用 `run_daily.py --source-limit 1` 做一次真实浏览器采集回归。
- [ ] 部署后确认服务器时区和 `Asia/Shanghai` 调度时间一致。

## 验收标准

1. 配置页只有一个入口。
2. 默认每天自动运行一次。
3. 同一天不会被调度器重复执行。
4. SQLite 只包含快照表和调度状态表。
5. 快照超过 7 天自动删除。
6. 配置检查、静态检查和单元测试通过。
